<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="healthscheduler.example.healthscheduler">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.HealthScheduler">
        <activity android:name="healthscheduler.example.healthscheduler.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="healthscheduler.example.healthscheduler.Login" />
        <activity android:name="healthscheduler.example.healthscheduler.MainActivity" />
        <activity android:name="healthscheduler.example.healthscheduler.Register" />
    </application>

</manifest>